node_modules/

# Local machine config and secrets. There is no build step and no backend, so
# there is no build output or env file today — these are here so that stays true
# by accident-prevention rather than by luck.
.env
.env.*
.claude/settings.local.json

# OS junk
.DS_Store
Thumbs.db
